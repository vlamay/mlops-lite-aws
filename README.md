# MLOps Lite on AWS

Single-repo, cost-guarded MLOps pipeline on AWS: Terraform IaC, serverless inference, training/validation, drift detection.

## Quick start
1. Configure AWS CLI
2. Initialize Terraform in `infra/terraform`
3. Apply infra and deploy Lambda

See `docs/` for architecture, runbook, and cost controls.
